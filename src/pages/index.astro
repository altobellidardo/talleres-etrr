---
import Layout from "../layouts/Layout.astro"
import Header from "../components/Header.astro";
import { SignedIn, SignedOut, SignInButton } from "@clerk/astro/components";
import SignedInPage from '../components/SignedIn.astro'
---

<Layout>
	<Header />
	
	<nav class="flex flex-col items-center gap-4 mt-10">
		<SignedOut>
			<p class="text-lg">
				Inicia sesion con tu cuenta de Microsoft para subscribirte a un taller
			</p>
			<SignInButton
				mode="modal"
				class:list={['px-8', 'py-1', 'rounded', 'text-white', 'bg-blue-500', 'hover:bg-blue-600']}
			>
				Iniciar sesión
			</SignInButton>
		</SignedOut>
		<SignedIn>
			<SignedInPage>
		</SignedIn>
	</nav>
</Layout>

<!-- <style>
	nav {
		display: flex;
		flex-direction: column;
		align-items: center;
		gap: 1rem;
		margin-top: 4rem;

		p {
			font-size: 1rem;
			margin: 0;
			color: var(--etrr-white);
		}
	}

	button {
		padding: 0.5rem 1rem;
		background-color: #fff;
		border: none;
		border-radius: 0.3rem;
		font-weight: 700;

		&:hover {
			background-color: var(--etrr-blue);
		}
	}
</style> -->